nuxt generate --no-ssr

Di nuxt.config.js:

export default {
  fetch: {
    client: true,
    server: false
  }
}

potongan status OPEN,CLOSE,DONE

*taruh di htdocs/logistik
.htaccess
<IfModule mod_rewrite.c>
  RewriteEngine On

  # Jangan rewrite file & folder nyata
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Semua selain itu → index.html
  RewriteRule . /logistik/index.html [L]
</IfModule>
 

*untuk hapus file2 tidak terpakai
rmdir /s /q _nuxt 



UPDATE extra_money_trx
SET attachment_1_loc = REPLACE(attachment_1_loc, '/files/', '')

